extends layout

block layout-content
    div.View.UserView
        h1.Banner Howdy, #{myPug.name}!
        h1.Banner Email, #{myPug.email} !
        div.Message
            div.Title
                h3 Making Us the Best
                h1 Teammate Profile
        div.NavButtons
            a(href="/logout")
                div.NavButton Log out        
